using System;

namespace Inventory.Model
{
    //internal class ApplicationServices : IApplicationServices
    //{
    //    public IUserProvider UserProvider { get; }
    //    public ILogger Logger { get; }
    //    public IDateProvider DateProvider { get; }

    //    public ApplicationServices(IUserProvider userProvider, ILogger logger, IDateProvider dateProvider)
    //    {
    //        UserProvider = userProvider ?? throw new ArgumentNullException(nameof(userProvider));
    //        Logger = logger ?? throw new ArgumentNullException(nameof(logger));
    //        DateProvider = dateProvider ?? throw new ArgumentNullException(nameof(dateProvider));
    //    }
    //}
}
